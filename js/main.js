// PDF buttons — ativa quando o arquivo existir
document.querySelectorAll('.btn-pdf[data-pending="true"]').forEach(btn => {
  btn.title = 'PDF em breve';
});

// PDF buttons — ativa quando o arquivo existir
document.querySelectorAll('.btn-pdf[data-pending="true"]').forEach(btn => {
  btn.title = 'PDF em breve';
});
